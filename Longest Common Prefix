class Solution:
    def longestCommonPrefix(self,strs):
        if not strs:
            return ""
        prefix=strs[0]
        for i in range(len(prefix)):
            char=prefix[i]
            for word in strs[1:]:
                if i>=len(word) or word[i]!=char:
                    return prefix[:i]
        return prefix
